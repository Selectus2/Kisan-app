<!DOCTYPE html>
<html>
  <head>
    <title>Kisan</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href='//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap-combined.min.css' rel='stylesheet' />
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <p class="navbar-text float-right">
    <% if farmer_signed_in? %>
      Logged in as <strong><%= current_farmer.email %></strong>.
    <%= link_to 'Edit profile', edit_farmer_registration_path, :class => 'navbar-link' %> |
    <%= link_to "Logout", destroy_farmer_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
    <%= link_to "Sign up", new_farmer_registration_path, :class => 'navbar-link'  %> |
    <%= link_to "Login", new_farmer_session_path, :class => 'navbar-link'  %>
    <% end %>
</p>
    <%= yield %>
  </body>
</html>
