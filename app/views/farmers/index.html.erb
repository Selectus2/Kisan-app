<!DOCTYPE html>
<html>
<head>
	<title></title>
	<meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
</head>
<body>
<div class="container">
	<h1 text-align: center;>Instruments</h1>
	<br>
	<button><%=	 link_to 'Add new Farmer',new_farmer_path %></button>
	<br>
	<br>
	<table class="table table-hover">
		<tr>
			<th>Name</th>
			<th>Mobile Number</th>
			<th>Email</th>
			<th>Edit Record</th>
			<th>Delete Record</th>
		</tr>

		<% @farmers.each do |farmer_entry| %>
			<tr>
				<td><%= farmer_entry.full_name %></td>
				<td><%= farmer_entry.mobile_number %></td>
				<td><%=  farmer_entry.email%></td>
				<td><%=	 link_to 'Edit',edit_farmer_path(farmer_entry.id) %></td>
				<td><%= link_to 'Delete Advertisement', farmer_path(farmer_entry.id),method: :delete,data: { confirm: 'Are You sure?' } %></td>
			</tr>
		<% end %>
	</table>
</div>
</div>
</body>
</html>
