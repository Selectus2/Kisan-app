<!DOCTYPE html>
<html>
<head>
	<title></title>
	<meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
<body>
	<div class="container-fluid">
	<h1>Instrument Details</h1>

	<h5>Name: <%= @instrument_data.name %> </h5>
	<h5>Brand: <%= @instrument_data.brand %> </h5>
	<h5>Specification: <%= @instrument_data.specification %></h5>
	<h5>Picture</h5>
	<%= image_tag(@instrument_data.image_url, :width => 600) if @instrument_data.image.present? %>
	</div>
</body>
</html>
